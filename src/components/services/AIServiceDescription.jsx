import React from "react";

export default function AIServiceDescription({ defaultDesc }) {
  return (
    <p className="text-[15px] text-muted-foreground leading-relaxed">
      {defaultDesc}
    </p>
  );
}
